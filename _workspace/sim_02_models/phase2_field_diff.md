# Phase 2 Field Diff

Machine-readable append-only diff from Phase 1 → Phase 2 for
`sim-engine-engineer`. Every row here is a NEW thing the engine has to
populate or may read. Defaults mean "Phase 1 constructors already work".

Generated by: `sim-model-designer` (task `sim_02_models_phase2_complete`)
Date: 2026-04-14
Source of truth for values: `spot-simulator/models/` package.

## `AgentState` — appended fields

| field             | type        | default        | mutable by                                             |
|-------------------|-------------|----------------|--------------------------------------------------------|
| `trust_score`     | `float`     | `0.5`          | Phase 3 settlement; Phase 2 reads only (see §3.5)      |
| `prev_trust`      | `float`     | `0.5`          | Phase 3 settlement snapshot; Phase 2 keeps == trust_score at init |
| `confirmed_spots` | `list[str]` | `[]` factory   | engine/lifecycle on SPOT_CONFIRMED; cleared on CHECK_IN / NO_SHOW / CANCEL_JOIN |
| `checked_in_spots`| `set[str]`  | `set()` factory| engine/lifecycle on CHECK_IN action                     |
| `noshow_spots`    | `set[str]`  | `set()` factory| engine/lifecycle on NO_SHOW detection                   |

### Methods added

| method                      | signature             | returns | notes |
|-----------------------------|-----------------------|---------|-------|
| `checked_in_for`            | `(self, spot_id: str)`| `bool`  | Membership in `checked_in_spots`. Used by Phase 3 `process_settlement` (plan §4.3). |

## `Spot` — appended fields

| field                | type           | default         | mutable by                                |
|----------------------|----------------|-----------------|-------------------------------------------|
| `duration`           | `int`          | `2`             | set at Spot construction (data-integrator / engine); 1~3 realistic per plan §3.4 |
| `confirmed_at_tick`  | `int \| None`  | `None`          | engine/lifecycle on MATCHED→CONFIRMED     |
| `started_at_tick`    | `int \| None`  | `None`          | engine/lifecycle on CONFIRMED→IN_PROGRESS |
| `completed_at_tick`  | `int \| None`  | `None`          | engine/lifecycle on IN_PROGRESS→COMPLETED |
| `disputed_at_tick`   | `int \| None`  | `None`          | engine/lifecycle on IN_PROGRESS→DISPUTED; read by Phase 3 `resolve_disputes` (§4.5) |
| `canceled_at_tick`   | `int \| None`  | `None`          | engine/lifecycle on cancel (OPEN timeout OR CONFIRMED all-cancel) |
| `checked_in`         | `set[str]`     | `set()` factory | engine/lifecycle on CHECK_IN action; `len` == plan §3.4 `count_checked_in(spot)` |
| `noshow`             | `set[str]`     | `set()` factory | engine/lifecycle on completion-time detection |

## `SpotStatus` — appended enum values

Append-only (all existing values preserved):

| value         | transitions in                                                            |
|---------------|---------------------------------------------------------------------------|
| `CONFIRMED`   | from `MATCHED` when `spot.scheduled_tick - tick <= 2`                     |
| `IN_PROGRESS` | from `CONFIRMED` when `tick >= spot.scheduled_tick`                       |
| `COMPLETED`   | from `IN_PROGRESS` when `tick >= scheduled_tick + duration` and noshow_ratio <= 0.5 |
| `DISPUTED`    | from `IN_PROGRESS` when noshow_ratio > 0.5                                |

Phase 3 will append `SETTLED`, `FORCE_SETTLED` (reserved, not added yet).

## `EventLog` — no structural change

`EventLog` dataclass fields are unchanged. Phase 2 adds only new
`event_type` string values. Import the authoritative set from `models`:

```python
from models import PHASE2_EVENT_TYPES
```

```python
PHASE2_EVENT_TYPES = {
    "CANCEL_JOIN",
    "CHECK_IN",
    "NO_SHOW",
    "COMPLETE_SPOT",
    "SPOT_TIMEOUT",
    "SPOT_CONFIRMED",
    "SPOT_STARTED",
    "SPOT_COMPLETED",
    "SPOT_DISPUTED",
}
```

| event_type       | agent | spot | lifecycle trigger / action           |
|------------------|-------|------|--------------------------------------|
| `CANCEL_JOIN`    | yes   | yes  | agent action: leave a joined spot    |
| `CHECK_IN`       | yes   | yes  | agent action at scheduled_tick       |
| `NO_SHOW`        | yes   | yes  | lifecycle: agent did not CHECK_IN in time |
| `COMPLETE_SPOT`  | yes   | yes  | agent action: host marks complete (optional — engine may use SPOT_COMPLETED instead) |
| `SPOT_TIMEOUT`   | None  | yes  | lifecycle: OPEN 48h cancel           |
| `SPOT_CONFIRMED` | None  | yes  | lifecycle: MATCHED → CONFIRMED       |
| `SPOT_STARTED`   | None  | yes  | lifecycle: CONFIRMED → IN_PROGRESS   |
| `SPOT_COMPLETED` | None  | yes  | lifecycle: IN_PROGRESS → COMPLETED   |
| `SPOT_DISPUTED`  | None  | yes  | lifecycle: IN_PROGRESS → DISPUTED    |

## Reads required by Phase 2 `calc_social_join_modifier` (plan §3.5)

For sim-engine-engineer to implement the social modifier, these fields are
now available:

- `host.trust_score` — plan §3.5 uses this directly
- `spot.participants` — already Phase 1
- `spot.capacity` — already Phase 1
- `agent.interest_categories` — already Phase 1

No additional model work required for §3.5; it only *reads*.

## What is NOT in scope for Phase 2 model

Plan §4 Phase 3 fields — explicitly NOT added:
- `AgentState.trust_threshold`
- `Spot.avg_satisfaction`
- `Spot.noshow_count`
- `SpotStatus.SETTLED`, `SpotStatus.FORCE_SETTLED`
- `event_type` values `SPOT_SETTLED`, `FORCE_SETTLED`, `REVIEW_WRITTEN`, `DISPUTE_RESOLVED`

These are sim-model-designer's Phase 3 responsibility.

## Default-choice rationale (why Phase 1 still works)

Every new field has a default value or default factory. Phase 1 code paths
(`main.py --phase 1`, `analysis.run_validate --phase 1`) construct
`AgentState` and `Spot` with positional/keyword args that name only Phase 1
fields; the new fields are not referenced anywhere in Phase 1 code paths,
so byte-identical output is preserved (verified: 630 total events,
CREATE_SPOT=210, JOIN_SPOT=247, SPOT_MATCHED=122, GATE PASS).
