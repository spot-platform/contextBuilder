# Phase 3 Field Diff

Machine-readable append-only diff from Phase 2 → Phase 3 for
`sim-engine-engineer`. Every row here is a NEW thing the engine has to
populate or may read. Defaults mean "Phase 1/2 constructors already work".

Generated by: `sim-model-designer` (task `sim_02_models_phase3_complete`)
Date: 2026-04-14
Source of truth for values: `spot-simulator/models/` package.

## `AgentState` — appended fields

| field                  | type          | default        | mutable by                                                        |
|------------------------|---------------|----------------|-------------------------------------------------------------------|
| `trust_threshold`      | `float`       | `0.5`          | data-integrator (init from persona); read by Phase 3 `calculate_satisfaction` (plan §4.4) |
| `review_spots`         | `list[str]`   | `[]` factory   | engine/settlement on WRITE_REVIEW emit                            |
| `saved_spots`          | `list[str]`   | `[]` factory   | engine/decision on SAVE_SPOT action                               |
| `satisfaction_history` | `list[float]` | `[]` factory   | engine/settlement append per settled spot the agent participated in |

## `Spot` — appended fields

| field              | type            | default | mutable by                                                                 |
|--------------------|-----------------|---------|----------------------------------------------------------------------------|
| `avg_satisfaction` | `float \| None` | `None`  | engine/settlement.process_settlement (plan §4.3); read by resolve_disputes (§4.5) |
| `noshow_count`     | `int`           | `0`     | engine/settlement at settlement time; mirrors `len(spot.noshow)`. Plan §4.4 reads this directly |
| `settled_at_tick`  | `int \| None`   | `None`  | engine/settlement on COMPLETED→SETTLED, DISPUTED→SETTLED, DISPUTED→FORCE_SETTLED |
| `force_settled`    | `bool`          | `False` | engine/settlement on the 24h dispute-timeout path (§4.5)                   |
| `review_count`     | `int`           | `0`     | engine/settlement increment per WRITE_REVIEW emit                          |

## `SpotStatus` — appended enum values

Append-only (all existing values preserved):

| value           | transitions in                                                                              |
|-----------------|---------------------------------------------------------------------------------------------|
| `SETTLED`       | from `COMPLETED` via `process_settlement`; or from `DISPUTED` via 6h rule when avg_satisfaction >= 0.5 |
| `FORCE_SETTLED` | from `DISPUTED` via 24h dispute-timeout rule (host trust penalized by 0.12)                |

## `EventLog` — no structural change

`EventLog` dataclass fields are unchanged. Phase 3 adds only new
`event_type` string values. Import the authoritative set from `models`:

```python
from models import PHASE3_EVENT_TYPES
```

```python
PHASE3_EVENT_TYPES = {
    "WRITE_REVIEW",
    "SETTLE",
    "SPOT_SETTLED",
    "FORCE_SETTLED",
    "DISPUTE_RESOLVED",
    "VIEW_FEED",
    "SAVE_SPOT",
}
```

| event_type         | agent | spot | trigger / action                                                  |
|--------------------|-------|------|-------------------------------------------------------------------|
| `WRITE_REVIEW`     | yes   | yes  | engine/settlement step 2: `random() < 0.3 + 0.4 * |sat - 0.5|`    |
| `SETTLE`           | yes   | yes  | host action wrapper around process_settlement                     |
| `SPOT_SETTLED`     | None  | yes  | lifecycle: COMPLETED→SETTLED, or DISPUTED→SETTLED via 6h rule     |
| `FORCE_SETTLED`    | None  | yes  | lifecycle: DISPUTED→FORCE_SETTLED via 24h timeout                  |
| `DISPUTE_RESOLVED` | None  | yes  | lifecycle: resolve_disputes 6h rule, before SPOT_SETTLED          |
| `VIEW_FEED`        | yes   | None | agent action: feed view (no spot binding)                         |
| `SAVE_SPOT`        | yes   | yes  | agent action: bookmark a spot                                     |

## NEW dataclasses

### `models.settlement.SettlementResult`

```python
from models import SettlementResult

SettlementResult(
    spot_id="S_0001",
    completed_count=3,
    noshow_count=1,
    avg_satisfaction=0.72,
    host_trust_delta=0.05,
    status="SETTLED",         # or "FORCE_SETTLED"
    settled_at_tick=42,
)
```

Returned by `engine.settlement.process_settlement(spot, agents, tick)`.
`status` is a string (not the SpotStatus enum) so `dataclasses.asdict`
yields a JSON-safe payload without a custom encoder.

### `models.settlement.Review`

```python
from models import Review

Review(
    reviewer_agent_id="A_001",
    spot_id="S_0001",
    satisfaction=0.78,
    tick=42,
)
```

Returned by `engine.settlement.generate_review(agent, spot, satisfaction)`
(plan §4.3 step 2). Lifted into a `WRITE_REVIEW` EventLog row by
`make_review_event(tick, agent, spot, review)`.

## Reads required by Phase 3 `calculate_satisfaction` (plan §4.4)

For sim-engine-engineer to implement the satisfaction function, these fields
are now available:

- `agent.interest_categories` — already Phase 1
- `agent.trust_threshold` — Phase 3 (NEW)
- `spot.category` — already Phase 1
- `spot.capacity` — already Phase 1
- `spot.checked_in` — already Phase 2 (`len(spot.checked_in)` for the
  ideal-ratio term)
- `spot.noshow_count` — Phase 3 (NEW); engine MUST set this from
  `len(spot.noshow)` BEFORE invoking `calculate_satisfaction`
- `spot.participants` — already Phase 1
- `spot.host_agent_id` — already Phase 1
- `agents[host_id].trust_score` — already Phase 2

## Reads required by `resolve_disputes` (plan §4.5)

- `spot.status` — already Phase 1
- `spot.disputed_at_tick` — already Phase 2
- `spot.avg_satisfaction` — Phase 3 (NEW); read by the 6h rule
- `agents[host_id].trust_score` — already Phase 2
- Sets `spot.status`, `spot.settled_at_tick`, `spot.force_settled` — all
  Phase 3

## Reads required by `process_settlement` (plan §4.3)

- `spot.participants` — already Phase 1
- `spot.host_agent_id` — already Phase 1
- `agent.checked_in_for(spot.spot_id)` — already Phase 2 (method)
- `host.trust_score`, `host.prev_trust` — already Phase 2
- `agent.trust_score` — already Phase 2 (for participant noshow penalty)
- Writes `spot.avg_satisfaction`, `spot.noshow_count`, `spot.review_count`,
  `spot.settled_at_tick`, `spot.status` — all Phase 3
- Writes `agent.review_spots`, `agent.satisfaction_history` — Phase 3
- Returns `SettlementResult` — Phase 3 (NEW dataclass)

## What is NOT in scope for Phase 3 model

Per task constraints, sim-model-designer does NOT implement:

- `engine.settlement.process_settlement` — sim-engine-engineer
- `engine.settlement.calculate_satisfaction` — sim-engine-engineer
- `engine.settlement.resolve_disputes` — sim-engine-engineer
- `engine.settlement.generate_review` — sim-engine-engineer
- `engine.settlement.make_review_event` — sim-engine-engineer

This module only provides the data structures those functions return /
construct.

## Default-choice rationale (why Phase 1 + Phase 2 still work)

Every new field has a default value or default factory. Phase 1/2 code
paths construct `AgentState` and `Spot` with positional/keyword args that
name only Phase 1/2 fields; the new fields are not referenced anywhere in
Phase 1/2 code paths, so byte-identical output is preserved:

- Phase 1: 630 events, CREATE_SPOT=210, JOIN_SPOT=247, SPOT_MATCHED=122,
  GATE PASS.
- Phase 2: 41,925 events,
  `md5(output/event_log.jsonl) == ea8c17ec0030e06f05e375f466bcbee3`,
  GATE PASS.
- pytest: 40 passed (38 Phase 2 baseline + 2 new Phase 3 drift sentinels).
