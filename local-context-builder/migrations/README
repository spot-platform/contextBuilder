Alembic migrations directory for local-context-builder.

- env.py reads the DB URL from app.config.Settings.database_url
- target_metadata is app.db.Base.metadata
- Concrete revision files live under migrations/versions/ and are
  generated by the schema-designer agent via `alembic revision --autogenerate`.
