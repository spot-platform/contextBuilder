[pytest]
testpaths = tests
markers =
    live_codex: calls real codex exec subprocess (skipped by default; runs only with -m live_codex)
    slow: longer-running tests
addopts = -m "not live_codex" -ra
python_files = test_*.py
python_classes = Test*
python_functions = test_*
